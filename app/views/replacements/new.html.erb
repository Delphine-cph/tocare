 <h1 class="text-center mb-4 mt-3">Tu souhaites te faire remplacer ? 😉</h1>
  <%= form_tag replacements_path, method:"post" do %>
    <div class="formulaire ">
      <div class="container">
        <div class="div-center text-center pb-3">
          <label>
            <strong>Du</strong> <%= date_field("replacement", "start_date") %>
          </label>
        </div>
        <div class="div-center mt-3 text-center">
          <label>
            <strong>Au</strong> <%= date_field("replacement", "end_date") %>
          </label>
        </div>
      </div>
      <%= submit_tag "Je valide !", class: "btn btn-gradient mt-5" %>
    </div>
  <% end %>
